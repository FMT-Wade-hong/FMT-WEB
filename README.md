# FMT-WEB

飛貓科技（FeiMao Tech）官方網站第一版，使用 React + Vite 建置。

## Local development

```bash
pnpm install
pnpm dev
```

## Build

```bash
pnpm build
```

預設建置路徑為 `/FMT-WEB/`，可透過 `VITE_BASE_PATH` 調整，方便日後切換自訂網域。
